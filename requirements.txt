# Smart Scan Strategy for EW — SIH 2026 PS 26055
# Install with: pip install -r requirements.txt

# Core runtime
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
pandas>=2.0
h5py>=3.11

# Demo / dashboard
streamlit>=1.30
plotly>=5.0

# ML utilities
scikit-learn>=1.3

# Testing & property-based testing
pytest>=7.4
hypothesis>=6.0

# Optional: HuggingFace Hub for downloading TSRD data
# pip install huggingface_hub>=0.24
# huggingface_hub>=0.24

# Optional: PyTorch for Neural-Q-Whittle (Module A roadmap item)
# Install separately — CPU-only:
#   pip install torch==2.3.1 --index-url https://download.pytorch.org/whl/cpu
# See requirements-torch.txt for the pinned version.

# Optional: official TSRD deinterleaving API
# Install from git:
#   pip install git+https://github.com/alan-turing-institute/turing-deinterleaving-challenge.git
